export interface Product {
    id: string;
    name: string;
    ref: string;
    price: number,
    status: string;
    marca: string;
    disponibility: 10,
    description: any[],
    image: any[],
}